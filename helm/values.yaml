# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: dmazek
  tag: "v1.0.5-a1274be-arm64"
  arch: amd64
secret:
  name: kbot
  env: TELE_TOKEN
  key: token
securityContext:
  priviliged: true